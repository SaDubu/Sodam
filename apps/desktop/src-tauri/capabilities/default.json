{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal main-window capability with no plugins or backend commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
