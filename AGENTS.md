# 프로젝트 설계 및 구현 준비 지침

## 적용 범위와 우선순위

이 지침은 이 저장소에서 수행하는 모든 프로젝트 설계·구현 준비 작업에 적용한다. 사용자 요청이 이 지침과 충돌할 경우 사용자의 최신 명시적 지시를 따른다.

## 승인 전 작업 절차

사용자가 프로젝트 아이디어 또는 요구사항을 설명하면 실제 기능 구현을 즉시 시작하지 않는다. 먼저 요구사항을 분석하고, 사용자가 검토할 상세 설계 문서를 `docs/ai/01-architecture.md`에 작성한다.

설계 문서를 작성한 뒤에는 멈추고 사용자의 승인을 기다린다. 사용자가 정확히 `APPROVE DESIGN`이라고 입력하기 전에는 다음 작업을 하지 않는다.

- 소스 코드, 테스트 코드, 설정 파일 생성 또는 수정
- 실제 비즈니스 로직, 알고리즘, 외부 API 호출, 데이터 변경 로직 구현
- 실제 테스트 로직 구현
- 커밋 또는 푸시
- 파일 삭제
- 외부 서비스 호출

승인 전에는 설계 문서와 사용자 요청에 의해 명시적으로 허용된 문서만 생성·수정할 수 있다.

## 설계 문서 필수 내용

`docs/ai/01-architecture.md`에는 다음을 반드시 포함한다.

1. 프로젝트 목표와 해결하려는 문제
2. 기능 범위와 구현하지 않을 범위
3. 전체 구조와 데이터 흐름
4. 생성·수정이 필요한 파일 목록과 각 파일의 책임
5. 사용할 라이브러리, 프레임워크, 외부 도구와 선택 이유
6. 구현해야 할 클래스, 함수, 인터페이스 목록
7. 각 함수의 입력, 출력, 예외, 부작용, 사전 조건, 사후 조건
8. 테스트 전략, 검증 방법, 자동화된 통과 기준 및 수동 검증 기준
9. 위험 요소, 미확정 사항, 사용자 결정이 필요한 항목

모든 클래스와 함수는 검증 가능한 계약 형태로 설계한다.

## 함수별 테스트 계약 및 도구 계획

설계 문서의 각 함수 계약에는 아래 항목을 반드시 명시한다.

- 테스트 대상 함수
- 정상 입력 사례
- 경계값 사례
- 잘못된 입력과 예상 예외
- Mock 또는 Fake가 필요한 외부 의존성
- 자동화된 통과 기준
- 사람이 확인해야 하는 수동 검증 기준
- 테스트 실행 명령

단위 테스트만 나열하지 않는다. 각 함수를 직접 호출해 입력값, 출력값, 예외, 부작용을 검증할 수 있는 테스트 도구 또는 테스트 헬퍼를 함께 설계한다. 필요에 따라 다음을 포함한다.

- 단위 테스트 코드
- Mock, Fake, Stub, Fixture
- 테스트 데이터 생성기
- CLI 기반 수동 검증 도구
- 샘플 입력 파일 생성기
- 결과 비교기 또는 검증기
- 성능 측정 도구
- 통합 테스트 실행 스크립트

테스트 도구는 제품 코드와 같은 수준으로 문서화하고 유지보수 가능하게 설계한다.

## `APPROVE DESIGN` 이후 작업 절차

사용자가 정확히 `APPROVE DESIGN`이라고 승인하면, 설계를 바탕으로 실제 기능 로직은 구현하지 않는다. 대신 코드 골격과 테스트 도구 골격만 작성한다.

골격에는 다음만 포함한다.

- 필요한 디렉터리와 파일
- 클래스, 함수, 타입, 인터페이스 선언
- 상세한 docstring과 주석
- 함수 또는 테스트 도구의 담당 기능과 동작 흐름
- 입력값, 반환값, 예외 처리 규칙
- Mock, Fixture, 테스트 데이터가 필요한 이유와 사용 방법
- 구현 완료 후 통과해야 할 테스트와 검증 기준
- `NotImplementedError` 또는 언어에 맞는 미구현 표시

이 단계에서도 실제 비즈니스 로직, 알고리즘, 외부 API 호출, 데이터 변경 로직, 실제 테스트 로직은 구현하지 않는다.

동시에 `docs/ai/04-implementation-order.md`를 작성한다.

## 구현 순서 문서 필수 내용

`docs/ai/04-implementation-order.md`에는 모든 함수 구현 작업과 테스트 도구 구현 작업을 의존성 순서대로 정리한다. 각 작업 항목에는 다음을 포함한다.

- 작업 ID
- 작업 종류: 함수 구현 / 테스트 도구 구현 / 테스트 코드 구현 / 문서화
- 대상 파일 경로
- 구현할 클래스, 함수 또는 테스트 도구 이름
- 선행 구현이 필요한 파일과 함수
- 구현 목적과 상세 동작
- 수정 허용 범위
- 금지 범위
- 테스트 방법
- 완료 판정 기준

구현 순서는 항상 선행 의존성이 없는 항목부터 시작한다. 모든 함수 구현 작업에는 해당 함수의 테스트 도구 또는 테스트 코드 작업을 연결한다. 함수 구현과 테스트 도구 구현은 독립된 작업으로 추적한다. 작업 완료는 관련 테스트 도구와 테스트 코드가 작성되고, 정의된 자동 테스트 및 검증 기준을 모두 통과했을 때에만 판정한다.

## 별도 명시 승인 필요 작업

커밋, 푸시, 파일 삭제, 외부 서비스 호출, 실제 함수 로직 구현, 실제 테스트 로직 구현은 사용자가 별도로 명시적으로 승인하기 전까지 수행하지 않는다.

## 실제 함수 구현 단계의 로컬 LLM 명세 절차

실제 함수 구현 단계에서는 로컬 LLM 모델 `qwen3.6:35b-a3b-agent-64k`를 함수 구현 도구로 사용한다. 이 모델에 구현을 지시할 때 일반적인 자연어 설명을 기준으로 사용하지 않는다. 프로젝트 루트의 `Statement_of_Functions.md`만을 구현 기준 명세로 사용한다.

각 구현 작업을 시작하기 전에 아래 절차를 순서대로 따른다.

1. `docs/ai/04-implementation-order.md`에서 선행 의존성이 모두 완료된 다음 작업 하나를 선택한다.
2. 기존 `Statement_of_Functions.md` 내용을 모두 초기화한다.
3. 선택한 하나의 작업에 대한 완전한 구현 명세만 `Statement_of_Functions.md`에 작성한다.
4. 명세 파일을 작성한 뒤 로컬 LLM에게 해당 파일을 먼저 읽고, 명세 범위 안에서만 구현하도록 지시한다.
5. 구현 결과가 명세의 완료 기준과 테스트 기준을 충족하는지 검증한다.
6. 검증 결과를 구현 순서 문서의 해당 작업 항목 또는 별도 작업 문서에 기록한 뒤 다음 작업의 명세를 작성한다.

`Statement_of_Functions.md`에는 한 번에 하나의 작업 또는 강하게 결합된 최소 작업 단위만 포함한다. 서로 독립적인 여러 함수의 구현을 하나의 명세 파일에 함께 넣지 않는다. 새 작업을 시작할 때 명세 파일은 초기화하되, 완료된 작업의 명세와 검증 결과는 반드시 `docs/ai/04-implementation-order.md` 또는 각 작업 문서에 남겨 추적 가능하게 한다.

## `Statement_of_Functions.md` 필수 명세 항목

각 작업 명세에는 다음 항목을 반드시 포함한다.

- 작업 ID
- 대상 파일 경로
- 구현할 함수, 클래스 또는 테스트 도구 이름
- 함수의 목적과 책임
- 함수 시그니처
- 입력값, 반환값, 예외
- 사전 조건과 사후 조건
- 내부 동작 순서
- 호출 가능한 의존 함수 및 금지된 의존성
- 수정 허용 파일과 수정 금지 파일
- 필요한 테스트 도구, Mock, Fixture, 테스트 데이터
- 실행해야 할 테스트 명령
- 자동 통과 기준
- 수동 검증 기준
- 구현하지 않아야 할 범위
- 완료 후 보고해야 할 내용

## 로컬 LLM 고정 지시문

로컬 LLM에는 항상 아래 형식으로 지시한다.

> `Statement_of_Functions.md`를 먼저 읽어라. 명세에 적힌 작업만 구현하라. 명세에 없는 파일이나 기능은 수정하지 마라. 구현 후 명세에 적힌 테스트를 실행하라. 테스트 결과, 수정한 파일, 남은 위험 요소를 보고하라. 커밋과 푸시는 하지 마라.
